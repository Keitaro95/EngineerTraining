 backend/refactored/ のRAG精度向上ロジックを洗い出しました。大きく 11カテゴリ
  に分類できます。

  ---
  1. フラット化（階層 → 1行1意味）

  ┌──────────────────────────────┬────────────────────────────────────────────────────┐
  │           ファイル           │                        概要                        │
  ├──────────────────────────────┼────────────────────────────────────────────────────┤
  │ parsers/pir_parser.py:64-603 │ Excel F/G列をブロックID単位で再帰パース、layer     │
  │                              │ 2/3/4のネスト構造を抽出                            │
  ├──────────────────────────────┼────────────────────────────────────────────────────┤
  │ parsers/to_parser.py:206-245 │ level_map + level_stack                            │
  │                              │ で任意深さの階層を正規化レベルに変換               │
  ├──────────────────────────────┼────────────────────────────────────────────────────┤
  │ parsers/yss_parser.py:47-114 │ 変更前/変更後を句点。で文分割し、1文＝1行に        │
  └──────────────────────────────┴────────────────────────────────────────────────────┘

  2. チャンキング（1行1ベクトル）

  - rag/vector_store.py:82-126 — _flatten_json
  が作業内容の各テキスト行を1チャンクに分解。チャンクIDは {doc_id}:{path}:{line_index}
  で一意。メタデータに階層パス・ステップ名・ノード種別を保持

  3. 埋め込み

  - rag/embeddings.py:23-312 — intfloat/multilingual-e5-small を使用。PyTorch / ONNX
  切替可。全ベクトルをL2正規化してコサイン類似度計算に最適化

  4. ベクトルDB

  - rag/vector_store.py:36-80 —
  ChromaDB（インメモリ）。ID・テキスト・メタデータ・事前計算済み埋め込みをセットで格納

  5. 検索（4層戦略）

  - search/engine.py:48-132 — 優先度順に：
    a. IRAN検索（完全一致 → 即リターン）
    b. TO/SN検索（TO番号・トリプルSN一致）
    c. キーワード検索（scorer.pyのスコアリング）
    d. 部品番号検索（完全一致）

  6. キーワードスコアリング

  - search/scorer.py:20-77 — score = [(coverage + density) / 2] × size_penalty
    - coverage = ヒットKW数 / 全KW数
    - density = ヒット文字数 / テキスト長
    - size_penalty = log(KW数+1) / log(16)

  7. RAGベクトル検索

  - rag/vector_store.py:148-254 — クエリを同義語で展開して複数バリアント生成 →
  各バリアントで検索 → チャンクID単位で重複排除（最高スコア保持）→ 閾値0.80以上のみ採用

  8. 構造フィルタリング（リランキング）

  - diff/sentence_diff.py:417-491 — total_score = context_similarity × 0.6 +
  keyword_overlap × 0.4（コサイン類似度 +
  Jaccard係数の加重平均）で構造的に関連するヒットだけを残す
  - search/filter.py:71-105 — ファイル単位でスコア合算 → ランク付け

  9. テキスト正規化

  - utils/text/normalizer.py:48-112 — 全角→半角、改行除去、空白統一、括弧/句読点正規化、N
  FKC正規化。パース用と検索用で2段階の正規化

  10. プロンプト設計 & コンテキスト組立

  - llm/prompts/keyword_prompt.py — 部品名・作業動詞・部品番号を確信度付きJSONで抽出
  - llm/prompts/full_llm_update_prompt.py —
  旧TO/新TO/差分/元JOBマニュアルの4点セットをLLMに渡す
  - diff/analyzer.py:113-269 —
  追加文には前後文脈をRAGで取得、変更文には対応する元文をRAGで取得し、LLMに渡す

  11. レスキュー処理

  - core/workflow.py:502-514 —
  RAG閾値未達の結果に対し、フルLLMプロセッサで再処理するフォールバック

  ---
  要するに、精度を高めている核心は：
  1. 1行1ベクトルのチャンキング（ノイズ排除）
  2. 同義語展開 + 重複排除のクエリ戦略
  3. 4層検索 + 構造スコアリングのリランキング
  4. 閾値ベースのフィルタリング（0.80）で低品質結果を排除
  5. 正規化の徹底で表記揺れを吸収